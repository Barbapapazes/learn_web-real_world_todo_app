<!DOCTYPE html>
<html lang="FR-fr">

  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/favicon.ico" type="image/ico">

    @entryPointStyles('app')
    @entryPointScripts('app')
  </head>

  <body class="h-screen flex flex-col">
    <header class="p-4 flex flex-row justify-between items-center border-b border-slate-300">
      <h1 class="text-2xl font-bold text-primary">
        <a href="/">
          Tâches de la journée
        </a>
      </h1>

      <a href="{{ route('login') }}"
        class="px-2 py-1 text-sm text-primary-light rounded-full border border-primary-light">
        Se connecter
      </a>
    </header>

    <main class="overflow-y-scroll flex-grow">
      @!section('content')
    </main>

    <section class="p-4 border-t border-slate-300">
      <form action="#" method="POST" class="flex flex-row justify-between items-center">
        <input type="text" placeholder="Saisir sa tâche"
          class="placeholder:text-base placeholder:text-primary-light w-full py-1 border-b border-primary">
        <button type="submit" class="ml-8 px-2 py-1 text-base text-white bg-primary rounded-full">Ajouter</button>
      </form>
    </section>
  </body>

</html>
